1) var={}
2)var={'foo':42}
3)dictionary has the ability to store key and pair values while lists cannot
4)Since there is no key value foo in spam it will give a keyerror
5)nothing really they both will check for cat as a keyy
6)just cat will check cat as key cat in spam.values will check if theres any key with the value cat
7)spam.setdefault('color','black')
8)pprint or pretty print is used so that the dictionary is more readable or looks better while viewing it